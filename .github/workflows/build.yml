name: Build Android

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Repository Check
        run: |
          echo "AI Level Generator Pro"
          ls -R

      - name: Upload Source
        uses: actions/upload-artifact@v4
        with:
          name: Source
          path: .
